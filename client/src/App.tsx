import AppApple from './AppApple'

/**
 * Phase 4 switches the default shell to the Apple HIG-inspired variant.
 * Keeping App.tsx as the entry wrapper avoids changing the rest of the app wiring.
 */
export default AppApple
